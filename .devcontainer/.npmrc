# Devcontainer-specific pnpm configuration
# Extends the root .npmrc with container-specific settings

# Use container volume for store to avoid WSL2/Windows filesystem permission issues
# This matches the volume mount in devcontainer.json
store-dir=/home/vscode/.pnpm-store
