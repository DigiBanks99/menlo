# Use copy instead of hardlink/reflink for WSL2 compatibility
package-import-method=copy
# Use native Linux filesystem for store to avoid WSL2 9p permission issues
store-dir=/tmp/pnpm-store
# Ensure optional dependencies are installed (required for esbuild platform binaries)
optional=true
